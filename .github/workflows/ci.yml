name: CI
on:
  push:
    branches:
      - '**'
  pull_request:
  workflow_call:

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - run: pip install pip-audit
      - run: pip-audit -r requirements.txt -r requirements-dev.txt

  audit-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci --prefix node_deps
      - run: npm audit --prefix node_deps --audit-level=high
        
  test-sqlite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - name: Setup config
        run: cp config.json.example config.json
      - run: pip install -r requirements.txt -r requirements-dev.txt
      - run: pytest tests/

  test-postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: myceladmin
          POSTGRES_PASSWORD: password
          POSTGRES_DB: mycel
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - name: Setup config
        run: cp config.json.example config.json
      - run: pip install -r requirements.txt -r requirements-dev.txt
      - run: pytest tests/
        env:
          TEST_DATABASE_URL: postgresql://myceladmin:password@localhost/mycel
