name: Release Backend

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  check-prerelease:
    runs-on: ubuntu-latest
    outputs:
      is_prerelease: ${{ steps.check.outputs.is_prerelease }}
    steps:
      - name: Check prerelease
        id: check
        run: |
          if [[ "${{ github.ref_name }}" == *"-beta"* ]] || [[ "${{ github.ref_name }}" == *"-rc"* ]] || [[ "${{ github.ref_name }}" == *"-alpha"* ]]; then
            echo "is_prerelease=true" >> $GITHUB_OUTPUT
          else
            echo "is_prerelease=false" >> $GITHUB_OUTPUT
          fi
          
  test:
    uses: ./.github/workflows/ci.yml

  release:
    needs: [check-prerelease, test]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Update VERSION file
        run: echo "${{ github.ref_name }}" | sed 's/^v//' > VERSION

      - name: Update VERSION and re-tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git fetch origin

          COMMIT=$(git rev-list -n 1 ${{ github.ref_name }})
          BRANCH=$(git branch -r --contains $COMMIT | grep -v 'HEAD' | head -n 1 | sed 's|.*origin/||' | xargs)

          if [ -z "$BRANCH" ]; then
            BRANCH="main"
          fi

          echo "Pushing to branch: $BRANCH"

          git checkout $BRANCH
          git pull origin $BRANCH

          echo "${{ github.ref_name }}" | sed 's/^v//' > VERSION

          git add VERSION
          git commit -m "chore: bump VERSION to ${{ github.ref_name }}"

          git tag -f ${{ github.ref_name }}

          git push origin $BRANCH
          git push origin ${{ github.ref_name }} --force

      - name: Extract changelog
        id: changelog
        run: |
          VERSION="${{ github.ref_name }}"
          VERSION_NO_V=$(echo "$VERSION" | sed 's/^v//')
          NOTES=$(awk "/^## ($VERSION|$VERSION_NO_V)/{flag=1; next} /^## /{flag=0} flag" CHANGELOG.md | tr -d '\r')
          if [ -z "$NOTES" ]; then
            NOTES="No release notes found for $VERSION"
          fi
          echo "notes<<EOF" >> $GITHUB_OUTPUT
          echo "$NOTES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          prerelease: ${{ needs.check-prerelease.outputs.is_prerelease }}
          name: ${{ github.ref_name }}
          body: ${{ steps.changelog.outputs.notes }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate openapi.json
        run: |
          pip install -r requirements.txt
          python -c "
          import json
          from src.interfaces.rest.rest import Rest
          from src.core.app_infos import AppInfos
          app_infos=AppInfos()
          rest = Rest(app_infos=app_infos)
          with open('openapi.json', 'w') as f:
            json.dump(rest.app.openapi(), f, indent=2)
          "
          
      - name: Push to mycel-openapi repo
        uses: dmnemec/copy_file_to_another_repo_action@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.DOCS_DEPLOY_TOKEN }}
        with:
          source_file: 'openapi.json'
          destination_repo: 'mycel-project/mycel-openapi'
          destination_folder: '.'
          user_email: 'github-actions[bot]@users.noreply.github.com'
          user_name: 'github-actions[bot]'
          commit_message: 'contract: update to ${{ github.ref_name }}'
          
      - name: Tag mycel-openapi repo
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.DOCS_DEPLOY_TOKEN }}
          script: |
            await new Promise(r => setTimeout(r, 2000));
            const ref = await github.rest.git.getRef({
              owner: 'mycel-project',
              repo: 'mycel-openapi',
              ref: 'heads/main'
            });
            try {
              await github.rest.git.createRef({
                owner: 'mycel-project',
                repo: 'mycel-openapi',
                ref: 'refs/tags/${{ github.ref_name }}',
                sha: ref.data.object.sha
              });
            } catch (error) {
              if (error.status === 422) {
                console.log("Tag already exists on remote repo.");
              } else {
                throw error;
              }
            }
