Orchestrators coordinate services and build views for transfer to interfaces.
They are responsible for resolving learning units from node_id + slot parameters received from the API. When an action goes through a dedicated use case, the use case handles this resolution instead.
